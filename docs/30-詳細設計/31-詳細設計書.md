# 詳細設計書（Aurawork）
作成日: 2025-11-12

## 1. 画面項目（抜粋）
### 勤怠一覧
- 対象日(work_date), 出勤(clock_in), 退勤(clock_out), 休憩(break_minutes), 状態(status)

### 休暇申請
- 区分(type), 開始日(start_date), 終了日(end_date), 日数(days), 理由(reason)

## 2. API詳細（抜粋）
- GET /api/time-entries?from&to
- POST /api/time-entries
- GET /api/leave-requests
- POST /api/leave-requests
- POST /api/pay-runs
- GET /api/pay-slips/{id}

## 3. バリデーション
email一意(会社内)/ 期間チェック / 年休残高超過バリデーション

## 4. モデル関連
User hasMany TimeEntry / PayRun hasMany PaySlip / PaySlip hasMany PayLine

## 5. エラーハンドリング
Problem+JSON風レスポンス、422詳細、403/404整備
